<h2>User Profile for </h2>

<% wineTasting.forEach(function(a) { %>
<div class="tasting">
    <h4>
        <%= a.notes %>
    </h4>
    <a href="/winetasting/<%= a.id %>">View this tasting note</a>
</div>
<br />
<% }) %>
